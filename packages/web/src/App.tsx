import type { JSX } from 'react';

/**
 * The application shell's placeholder screen. It renders the product name only;
 * the visual design arrives with the first UI feature's impeccable subphase
 * (decision-log [14]).
 */
export function App(): JSX.Element {
  return <h1>chrysalyst</h1>;
}

# Secrets / local env
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code — per-user, not shared
.claude/settings.local.json

# Build / dependency / tooling output
node_modules/
dist/
coverage/
.vite/
target/

# impeccable — transient decision-server state and generated mocks
.impeccable/questions/
.impeccable/mocks/

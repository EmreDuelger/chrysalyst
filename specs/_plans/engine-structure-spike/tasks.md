# Tasks: engine-structure-spike

## Phase 2: Implementation (Group A — scaffolding, ordered)
- [x] 1. Branch and skeleton: `spike/engine-structure/` with its own `pnpm-workspace.yaml` (`packages: []`), `package.json` linking `@chrysalyst/core` + `@chrysalyst/server`, `tsconfig.json`, `vitest.config.ts` (`**/*.spike.test.ts` only). Root `eslint.config.js` + `.prettierignore` branch-only edits. Prove isolation: `git status` clean at repo root, `pnpm -r --include-workspace-root test` at repo root still passes. STOP if isolation fails.
- [x] 2. Shared harness ground: `spike/engine-structure/shared/` — store factory (`createFilesystemSessionStore`, `CHRYSALYST_SESSION_DIR` default `~/.chrysalyst-spike/<arm>`, never `~/.chrysalyst/sessions`), real-LLM factory, shared fake `LlmPort`, opening instruction copied from `OPENING_SYSTEM_PROMPT`, metrics recorder. Confirm deep-path imports resolve (vendoring fallback if not). Smoke-test save/load with no model.

## Phase 2: Implementation (Group B — arm A, ordered)
- [x] 3. Arm A hand-rolled turn loop: `spike/engine-structure/arm-a/` adapting `createSingleTurnInterview` logic. Direct `SessionStorePort.save`/`load`, no abstraction. No in-flight guard / shared-production cancellation.
- [x] 4. Arm A harness + both runs: `arm-a/round.spike.test.ts` (fake LlmPort + mkdtemp), `arm-a/live-ask.ts` + `arm-a/live-answer.ts` (two processes, real Ollama qwen3:8b). Capture `session.json` verbatim. Record axis 1 (LOC, concepts, failed attempts) + axis 4 hermetic answer.

## Phase 2: Implementation (Group C — arm B, ordered, parallel with Group B; NOT concurrent with Group B's live runs on Ollama)
- [x] 5. [expert] **Stopped under its LOC stop rule, artifact complete.** 334 non-blank/non-comment LOC (ceiling 300); `arm-b/checkpointer.spike.test.ts` passes 10/10; blocking member `abstract deleteThread(threadId: string): Promise<void>` — unsatisfiable, `SessionStorePort` declares no `delete`. Arm B checkpointer: Context7 first. `session-store-checkpointer.ts` — `BaseCheckpointSaver` subclass over real `SessionStorePort`. Document thread_id↔SessionId mapping, CheckpointTuple reconstitution, TState, putWrites, list. STOP at 300 non-blank/non-comment LOC or 3 failed cross-process-resume attempts; record reached state + blocking member verbatim as axis-1 measurement. A stop does not fail the plan.
- [x] 6. [expert] **Completed, did not stop.** Cross-process resume proved against Ollama `qwen3:8b` on the first attempt (0 failures); `arm-b/round-graph.ts` 93 LOC, +`live-ask.ts`/`live-answer.ts` 160 LOC (ceiling 300); `session.json` captured to `measurements/arm-b-session.json`. Arm B graph/interrupt/resume: Context7 first. `StateGraph` node asking opening question, `interrupt()`, resume node recording answer. Compile with task-5 checkpointer, drive with `ChatOllama`. Prove resume via `Command({ resume })` + same `thread_id` across two processes. STOP at 300 non-blank/non-comment LOC or 3 failed resume attempts; record blocking API verbatim. Tasks 7/8 still run after a stop.
- [x] 7. [expert] **Completed, did not stop.** `arm-b/llm-port-chat-model.ts` 70 LOC (ceiling 200); no `@langchain/core` internals reached — `_llmType` and `_generate` are the only abstract members, `_streamResponseChunks` the one documented override. Proven sufficient by `arm-b/binding-drives-node.check.ts` driving the whole round hermetically. Arm B guardrail-compliant binding: Context7 first. Minimal `BaseChatModel` subclass over `LlmPort`. STOP at 200 non-blank/non-comment LOC or reaching undocumented `@langchain/core` internals. Record LOC/outcome/blocking reason — this is the axis-4 decision number.
- [x] 8. Arm B harness + both runs: Context7 for `@langchain/core/utils/testing` fake chat models first. `arm-b/round.spike.test.ts` via task-7 binding if it completed, else LangChain fake chat model. Record axis-4 value as "achievable via <route>, at <LOC> cost" (reserve "not achievable" for no-route-exists). `arm-b/live-ask.ts` + `arm-b/live-answer.ts` mirroring task 4. Capture `session.json` verbatim.

## Phase 2: Implementation (Group D — measure, write, move; ordered, after B and C)
- [x] 9. Measure all four axes into `measurements/comparison.md`. "not obtained" only for metrics no one tried; a stopped arm B (task 5/6) is an axis-1 *result*, not "not obtained". Estimate nothing (axis 3 excepted, marked extrapolation).
- [ ] 10. [expert] Write `specs/_plans/engine-structure-spike/decision-note.md` in the `004` ADR skeleton (file title, single `## ADR:`, `**ID:** engine-structure-<handrolled|langgraph>`, `**Plan:**`, `**Status:** Accepted`, `### Context`/`### Decision`/`### Options Considered`/`### Consequences`). Cite § The decision rule by name, show each condition's evidence against its threshold/acceptance line. If LangGraph recommended: name the `Kein Agent-Framework in der Domäne` collision + proposed placement. State the rejected option's strongest case fairly.
- [ ] 11. Move the note to `main`: tag `spike/engine-structure-m3`, push tag (needs approval — `/speq:git-discipline`). Cut a branch from `main`, copy only `decision-note.md` + `verification-report.md` (+ `review/round-*.md` if any) into `specs/_plans/engine-structure-spike/`. Verify `git diff --stat main...<branch>` shows only `specs/` paths. Never merge `spike/engine-structure` into `main`.

## Phase 3: Verification
- [ ] 3.1 Run root checklist on spike branch (install, build, test, typecheck, lint, format, `speq plan validate`)
- [ ] 3.2 Manual Testing table (isolation × 3, arm A hermetic/live, arm B hermetic/live, axis 2, axis 1, deliverable, no-leakage, reachability)
- [ ] 3.3 Generate verification-report.md
